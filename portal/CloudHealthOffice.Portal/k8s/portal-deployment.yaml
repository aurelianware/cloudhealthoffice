apiVersion: v1
kind: ConfigMap
metadata:
  name: portal-config
  namespace: cho-svcs
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  Services__MemberService: "http://member-service.cho-svcs/api"
  Services__CoverageService: "http://coverage-service.cho-svcs/api"
  Services__ClaimsService: "http://claims-service.cho-svcs/api"
  Services__EligibilityService: "http://eligibility-service.cho-svcs/api"
  Services__AuthorizationService: "http://authorization-service.cho-svcs/api"
  Services__ProviderService: "http://provider-service.cho-svcs/api"
  Services__BenefitPlanService: "http://benefit-plan-service.cho-svcs/api"
  Services__ArgoWorkflows: "http://argo-workflows-server.cho-workflows:2746"
  Services__Prometheus: "http://cho-monitoring-prometheus-server.cho-monitoring"
  Portal__Title: "Cloud Health Office"
  Portal__Version: "1.0.0"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: portal
  namespace: cho-svcs
  labels:
    app: portal
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: portal
  template:
    metadata:
      labels:
        app: portal
        version: v1
    spec:
      containers:
      - name: portal
        image: ghcr.io/aurelianware/cloudhealthoffice-portal:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: ASPNETCORE_ENVIRONMENT
        - name: Services__MemberService
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: Services__MemberService
        - name: Services__CoverageService
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: Services__CoverageService
        - name: Services__ClaimsService
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: Services__ClaimsService
        - name: Services__EligibilityService
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: Services__EligibilityService
        - name: Services__AuthorizationService
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: Services__AuthorizationService
        - name: Services__ProviderService
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: Services__ProviderService
        - name: Services__BenefitPlanService
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: Services__BenefitPlanService
        - name: Services__ArgoWorkflows
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: Services__ArgoWorkflows
        - name: Services__Prometheus
          valueFrom:
            configMapKeyRef:
              name: portal-config
              key: Services__Prometheus
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: cho-svcs
  labels:
    app: portal
spec:
  type: LoadBalancer
  selector:
    app: portal
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: portal-hpa
  namespace: cho-svcs
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: portal
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
