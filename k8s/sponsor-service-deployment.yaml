apiVersion: v1
kind: ConfigMap
metadata:
  name: sponsor-service-config
  namespace: cloudhealthoffice
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  CosmosDb__DatabaseName: "CloudHealthOffice"
  CosmosDb__SponsorContainerName: "Sponsors"
---
apiVersion: v1
kind: Secret
metadata:
  name: sponsor-service-secrets
  namespace: cloudhealthoffice
type: Opaque
stringData:
  CosmosDb__Endpoint: "https://your-cosmos-account.documents.azure.com:443/"
  CosmosDb__Key: "your-cosmos-primary-key-here"
  # TODO: Replace with actual Azure Key Vault reference or managed identity
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sponsor-service
  namespace: cloudhealthoffice
  labels:
    app: sponsor-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sponsor-service
  template:
    metadata:
      labels:
        app: sponsor-service
        version: v1
    spec:
      containers:
      - name: sponsor-service
        image: your-acr.azurecr.io/sponsor-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: sponsor-service-config
              key: ASPNETCORE_ENVIRONMENT
        - name: CosmosDb__DatabaseName
          valueFrom:
            configMapKeyRef:
              name: sponsor-service-config
              key: CosmosDb__DatabaseName
        - name: CosmosDb__Endpoint
          valueFrom:
            secretKeyRef:
              name: sponsor-service-secrets
              key: CosmosDb__Endpoint
        - name: CosmosDb__Key
          valueFrom:
            secretKeyRef:
              name: sponsor-service-secrets
              key: CosmosDb__Key
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: sponsor-service
  namespace: cloudhealthoffice
  labels:
    app: sponsor-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: sponsor-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sponsor-service-hpa
  namespace: cloudhealthoffice
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sponsor-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
