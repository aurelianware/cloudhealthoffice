apiVersion: v1
kind: ConfigMap
metadata:
  name: sponsor-service-config
  namespace: cloudhealthoffice
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  MongoDb__DatabaseName: "cloudhealthoffice"
---
apiVersion: v1
kind: Secret
metadata:
  name: sponsor-service-secrets
  namespace: cloudhealthoffice
type: Opaque
stringData:
  MongoDb__ConnectionString: "mongodb://admin:securepassword123@mongodb:27017"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sponsor-service
  namespace: cloudhealthoffice
  labels:
    app: sponsor-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sponsor-service
  template:
    metadata:
      labels:
        app: sponsor-service
        version: v1
    spec:
      containers:
      - name: sponsor-service
        image: ghcr.io/aurelianware/cloudhealthoffice-sponsor-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: sponsor-service-config
              key: ASPNETCORE_ENVIRONMENT
        - name: MongoDb__ConnectionString
          valueFrom:
            secretKeyRef:
              name: sponsor-service-secrets
              key: MongoDb__ConnectionString
        - name: MongoDb__DatabaseName
          valueFrom:
            configMapKeyRef:
              name: sponsor-service-config
              key: MongoDb__DatabaseName
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: sponsor-service
  namespace: cloudhealthoffice
  labels:
    app: sponsor-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: sponsor-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sponsor-service-hpa
  namespace: cloudhealthoffice
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sponsor-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
