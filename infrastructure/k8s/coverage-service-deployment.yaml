apiVersion: v1
kind: ConfigMap
metadata:
  name: coverage-service-config
  namespace: cloudhealthoffice
data:
  ASPNETCORE_ENVIRONMENT: "Production"
---
apiVersion: v1
kind: Secret
metadata:
  name: coverage-service-secrets
  namespace: cloudhealthoffice
type: Opaque
stringData:
  MongoDb__ConnectionString: "mongodb://admin:securepassword123@mongodb:27017"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: coverage-service
  namespace: cloudhealthoffice
  labels:
    app: coverage-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coverage-service
  template:
    metadata:
      labels:
        app: coverage-service
        version: v1
    spec:
      containers:
      - name: coverage-service
        image: ghcr.io/aurelianware/cloudhealthoffice-coverage-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: MongoDb__ConnectionString
          valueFrom:
            secretKeyRef:
              name: coverage-service-secrets
              key: MongoDb__ConnectionString
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: coverage-service-config
              key: ASPNETCORE_ENVIRONMENT
        resources:
          requests:
            memory: "256Mi"
            cpu: "500m"
          limits:
            memory: "512Mi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: coverage-service
  namespace: cloudhealthoffice
  labels:
    app: coverage-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: coverage-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: coverage-service-hpa
  namespace: cloudhealthoffice
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: coverage-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
