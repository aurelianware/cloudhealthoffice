apiVersion: v1
kind: ConfigMap
metadata:
  name: provider-service-config
  namespace: cho-svcs
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  CosmosDb__DatabaseName: "ProviderDB"
  CosmosDb__ContainerName: "Providers"
---
apiVersion: v1
kind: Secret
metadata:
  name: provider-service-secrets
  namespace: cho-svcs
type: Opaque
stringData:
  CosmosDb__Endpoint: "https://your-cosmos-account.documents.azure.com:443/"
  CosmosDb__Key: "your-cosmos-key-here"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: provider-service
  namespace: cho-svcs
  labels:
    app: provider-service
    tier: backend
    component: provider-directory
spec:
  replicas: 2
  selector:
    matchLabels:
      app: provider-service
  template:
    metadata:
      labels:
        app: provider-service
        tier: backend
        component: provider-directory
    spec:
      containers:
      - name: provider-service
        image: ghcr.io/aurelianware/cloudhealthoffice-provider-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: provider-service-config
              key: ASPNETCORE_ENVIRONMENT
        - name: CosmosDb__DatabaseName
          valueFrom:
            configMapKeyRef:
              name: provider-service-config
              key: CosmosDb__DatabaseName
        - name: CosmosDb__ContainerName
          valueFrom:
            configMapKeyRef:
              name: provider-service-config
              key: CosmosDb__ContainerName
        - name: CosmosDb__Endpoint
          valueFrom:
            secretKeyRef:
              name: provider-service-secrets
              key: CosmosDb__Endpoint
        - name: CosmosDb__Key
          valueFrom:
            secretKeyRef:
              name: provider-service-secrets
              key: CosmosDb__Key
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: provider-service
  namespace: cho-svcs
  labels:
    app: provider-service
spec:
  type: ClusterIP
  selector:
    app: provider-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: provider-service-hpa
  namespace: cho-svcs
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: provider-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
