{
  "_comment": "Local development override. Provider=None uses NullSecretProvider — all secrets come from appsettings.json, user-secrets, or environment variables as usual. No Azure Key Vault connection is attempted.",

  "SecretProvider": {
    "Provider": "None"
  }
}
