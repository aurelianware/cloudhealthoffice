{
  "_comment": "Copy this section into each microservice's appsettings.json (or appsettings.Production.json) to enable Azure Key Vault. The SecretProvider:Provider value must be 'AzureKeyVault' and the AzureKeyVaultUri must point to the application Key Vault. DefaultAzureCredential handles auth automatically (Workload Identity in AKS, az login locally).",

  "SecretProvider": {
    "Provider": "AzureKeyVault",
    "AzureKeyVaultUri": "https://kv-cho-prod-eastus.vault.azure.net/",
    "ReloadIntervalSeconds": 300,
    "GracefulDegradation": true
  }
}
