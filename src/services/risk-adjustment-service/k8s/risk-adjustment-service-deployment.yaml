apiVersion: v1
kind: ConfigMap
metadata:
  name: risk-adjustment-service-config
  namespace: cloudhealthoffice
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  CosmosDb__DatabaseName: "CloudHealthOffice"
  CosmosDb__ContainerName: "RiskScores"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: risk-adjustment-service
  namespace: cloudhealthoffice
  labels:
    app: risk-adjustment-service
    tier: backend
    component: risk-adjustment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: risk-adjustment-service
  template:
    metadata:
      labels:
        app: risk-adjustment-service
        tier: backend
        component: risk-adjustment
    spec:
      containers:
      - name: risk-adjustment-service
        image: clouhealthoffice.azurecr.io/cloudhealthoffice-risk-adjustment-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: MongoDb__ConnectionString
          valueFrom:
            secretKeyRef:
              name: risk-adjustment-service-secret
              key: MongoDb__ConnectionString
              optional: true
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: risk-adjustment-service-config
              key: ASPNETCORE_ENVIRONMENT
        - name: CosmosDb__DatabaseName
          valueFrom:
            configMapKeyRef:
              name: risk-adjustment-service-config
              key: CosmosDb__DatabaseName
        - name: CosmosDb__ContainerName
          valueFrom:
            configMapKeyRef:
              name: risk-adjustment-service-config
              key: CosmosDb__ContainerName
        - name: CosmosDb__Endpoint
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: endpoint
        - name: CosmosDb__Key
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: key
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: risk-adjustment-service
  namespace: cloudhealthoffice
  labels:
    app: risk-adjustment-service
spec:
  type: ClusterIP
  selector:
    app: risk-adjustment-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: risk-adjustment-service-hpa
  namespace: cloudhealthoffice
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: risk-adjustment-service
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
