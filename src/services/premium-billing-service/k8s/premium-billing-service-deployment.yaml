apiVersion: v1
kind: ConfigMap
metadata:
  name: premium-billing-service-config
  namespace: cloudhealthoffice
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  CosmosDb__DatabaseName: "CloudHealthOffice"
  CoverageService__BaseUrl: "http://coverage-service.cloudhealthoffice"
  SponsorService__BaseUrl: "http://sponsor-service.cloudhealthoffice"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: premium-billing-service
  namespace: cloudhealthoffice
  labels:
    app: premium-billing-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: premium-billing-service
  template:
    metadata:
      labels:
        app: premium-billing-service
        tier: backend
    spec:
      containers:
      - name: premium-billing-service
        image: clouhealthoffice.azurecr.io/cloudhealthoffice-premium-billing-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: premium-billing-service-config
              key: ASPNETCORE_ENVIRONMENT
        - name: MongoDb__ConnectionString
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: connectionString
              optional: true
        - name: CosmosDb__Endpoint
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: endpoint
        - name: CosmosDb__Key
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: key
        - name: CosmosDb__DatabaseName
          valueFrom:
            configMapKeyRef:
              name: premium-billing-service-config
              key: CosmosDb__DatabaseName
        - name: CoverageService__BaseUrl
          valueFrom:
            configMapKeyRef:
              name: premium-billing-service-config
              key: CoverageService__BaseUrl
        - name: SponsorService__BaseUrl
          valueFrom:
            configMapKeyRef:
              name: premium-billing-service-config
              key: SponsorService__BaseUrl
        - name: Stripe__SecretKey
          valueFrom:
            secretKeyRef:
              name: stripe-api-keys
              key: secret-key
        - name: Stripe__WebhookSecret
          valueFrom:
            secretKeyRef:
              name: stripe-api-keys
              key: webhook-secret
              optional: true
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: premium-billing-service
  namespace: cloudhealthoffice
  labels:
    app: premium-billing-service
spec:
  type: ClusterIP
  selector:
    app: premium-billing-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
