apiVersion: v1
kind: ConfigMap
metadata:
  name: member-document-service-config
  namespace: cloudhealthoffice
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  MongoDb__DatabaseName: "cloudhealthoffice"
  BlobStorage__ContainerName: "member-documents"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: member-document-service
  namespace: cloudhealthoffice
  labels:
    app: member-document-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: member-document-service
  template:
    metadata:
      labels:
        app: member-document-service
        tier: backend
    spec:
      containers:
      - name: member-document-service
        image: clouhealthoffice.azurecr.io/cloudhealthoffice-member-document-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: member-document-service-config
              key: ASPNETCORE_ENVIRONMENT
        - name: MongoDb__ConnectionString
          valueFrom:
            secretKeyRef:
              name: database-secret
              key: connectionString
        - name: MongoDb__DatabaseName
          valueFrom:
            configMapKeyRef:
              name: member-document-service-config
              key: MongoDb__DatabaseName
        - name: BlobStorage__ConnectionString
          valueFrom:
            secretKeyRef:
              name: azure-storage-secret
              key: connectionString
        - name: BlobStorage__ContainerName
          valueFrom:
            configMapKeyRef:
              name: member-document-service-config
              key: BlobStorage__ContainerName
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: member-document-service
  namespace: cloudhealthoffice
  labels:
    app: member-document-service
spec:
  type: ClusterIP
  selector:
    app: member-document-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP